services:
  backend:
    image: python_app
    build:
      context: ./backend
    ports:
      - "8000:8000"

  frontend:
    image: js_app
    build: 
      context: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

networks:
  default:
    driver: bridge
